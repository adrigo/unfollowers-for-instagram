{
  "name": "ig-unfollowers",
  "version": "1.0.0",
  "description": "Secure, lightweight browser utility to detect Instagram unfollowers.",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --loader:.svg=text --loader:.css=text --minify --outfile=dist/index.min.js && node scripts/update-html.js",
    "dev": "esbuild src/index.ts --bundle --loader:.svg=text --loader:.css=text --outfile=dist/index.js --serve=3000",
    "package:extension": "npm run build && node scripts/create-extension-icons.js && (cd extension && zip -r ../extension.zip . -x extension.zip)"
  },
  "devDependencies": {
    "esbuild": "^0.20.2",
    "typescript": "^5.4.5"
  }
}
